import React from 'react';
export default function TaskCard({task,onChange}){return <div className={`task-card ${task.completed?'done':''}`}><button className="check" onClick={()=>onChange(task)} aria-label={task.completed?'Mark incomplete':'Mark complete'}>{task.completed?'✓':''}</button><div className="task-time">{task.time}</div><div className="task-body"><div className="task-top"><span className={`tag ${task.exam.toLowerCase()}`}>{task.exam}</span><span className="duration">{task.duration} min</span></div><h3>{task.title}</h3><p>{task.topic}</p><small>{task.description}</small></div></div>}
