import React from 'react';import {EXAMS,SYLLABUS} from '../data';
export default function Exams(){return <div className="page"><div className="page-title"><div><div className="eyebrow">EXAM CONTROL</div><h1>Your exams</h1><p>Pattern, dates, application details and section structure in one place.</p></div></div><div className="exam-grid">{EXAMS.map(e=><article className="exam-card" key={e.key}><div className="exam-card-head"><span className="tag">{e.stage}</span><span>{e.window}</span></div><h2>{e.name}</h2><p>{e.description}</p><div className="metric-row"><div><b>{e.marks}</b><span>marks</span></div><div><b>{e.duration}</b><span>duration</span></div><div><b>{e.subjects?.length||0}</b><span>sections</span></div></div><h4>Structure</h4><div className="subject-list">{e.subjects?.map((s,i)=><div key={i}><b>{s.name}</b>{s.marks&&<span>{s.questions} Q · {s.marks} marks · {s.duration||''}</span>}{s.topics&&<small>{s.topics.join(' · ')}</small>}</div>)}</div>{e.key==='SSC_P2'&&<div className="application"><b>Part-D coverage</b><span>{SYLLABUS.SSC.filter(([name])=>name.startsWith('Part-D')).length} technical areas are mapped in Syllabus.</span></div>}{e.application&&<div className="application"><b>Your application</b>{Object.entries(e.application).map(([k,v])=><span key={k}>{k}: {v}</span>)}</div>}<a className="resource-link" href={e.officialUrl} target="_blank" rel="noreferrer">Official source ↗</a></article>)}</div></div>}
