import React,{useMemo,useState} from 'react';import {RESOURCES} from '../data';
export default function Resources(){const [filter,setFilter]=useState('All');const filtered=useMemo(()=>filter==='All'?RESOURCES:RESOURCES.filter(x=>x.exam===filter),[filter]);return <div className="page"><div className="page-title"><div><div className="eyebrow">REFERENCE LIBRARY</div><h1>Study resources</h1><p>Official sources first, followed by focused technical references.</p></div><div className="seg">{['All','RRB','SSC'].map(x=><button key={x} className={filter===x?'selected':''} onClick={()=>setFilter(x)}>{x}</button>)}</div></div><div className="resource-grid">{filtered.map(r=><article className="resource-card" key={r.title}><div className="resource-meta"><span className="tag">{r.type}</span>{r.official&&<span className="official">Official</span>}</div><h3>{r.title}</h3><p>{r.subject} reference material for {r.exam}.</p><div className="resource-bottom"><span>{r.provider}</span><a href={r.url} target="_blank" rel="noreferrer">Open ↗</a></div></article>)}</div></div>}
