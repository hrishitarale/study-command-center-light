import React,{useRef,useState} from 'react';import {exportData,importData,getState,logProgress,resetAll} from '../store';
export default function Settings(){const [minutes,setMinutes]=useState('');const fileRef=useRef();const save=()=>{if(!minutes)return;logProgress({date:new Date().toISOString().slice(0,10),minutes:Number(minutes),exam:'Both',subject:'Study session',note:'Manual log'});setMinutes('');alert('Study time logged.');};const download=()=>{const blob=new Blob([exportData()],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`study-command-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)};const restore=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{importData(r.result);alert('Backup restored. Reloading…');location.reload()}catch{alert('That backup file is not valid.')}};r.readAsText(f)};const reset=()=>{if(confirm('Delete all local study data from this browser?')){resetAll();location.reload()}};const s=getState();return <div className="page"><div className="page-title"><div><div className="eyebrow">PERSONAL SETTINGS</div><h1>System controls</h1><p>Everything here is local to this browser.</p></div></div><div className="settings-grid"><section className="panel"><h3>Log study time</h3><p>Add time spent outside the generated task blocks.</p><div className="inline"><input type="number" min="1" value={minutes} onChange={e=>setMinutes(e.target.value)} placeholder="Minutes"/><button className="primary" onClick={save}>Log</button></div></section><section className="panel"><h3>Current schedule</h3><p><b>10:30 AM–1:00 PM</b> morning study</p><p><b>7:30 PM–1:00 AM</b> night study</p><p>Topic rotation changes by day; exam dates are shown from the app's study data.</p></section><section className="panel"><h3>Backup</h3><p>Export your tasks, progress, mistakes, mocks and syllabus tracking before clearing browser data or changing devices.</p><div className="inline"><button className="primary" onClick={download}>Export JSON</button><button className="ghost" onClick={()=>fileRef.current?.click()}>Import</button><input ref={fileRef} hidden type="file" accept="application/json" onChange={restore}/></div></section><section className="panel"><h3>Local lock</h3><p>Your PIN is hashed and stored locally. It does not protect the source code or create server-side authentication.</p><p><small>To change the PIN, clear the app's local data and set it again.</small></p></section><section className="panel danger"><h3>Reset app data</h3><p>This removes all locally stored study data from this browser.</p><button className="danger-button" onClick={reset}>Reset everything</button></section></div></div>}
